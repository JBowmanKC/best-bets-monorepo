{
  "name": "best-bets-monorepo",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/algorithm",
    "api"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "npm run build --workspace=packages/algorithm",
    "dev": "npm run build --workspace=packages/algorithm && npm run dev --workspace=apps/web",
    "dev:api": "npm run build --workspace=packages/algorithm && tsx watch scripts/dev-api.mts",
    "build": "npm run build --workspace=packages/algorithm && npm run build --workspace=apps/web",
    "typecheck": "npm run build --workspace=packages/algorithm && tsc --noEmit",
    "lint": "npm run lint --workspace=apps/web"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.4.5"
  }
}
